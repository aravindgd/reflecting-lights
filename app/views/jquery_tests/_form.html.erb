<%= form_for(@jquery_test) do |f| %>
  <% if @jquery_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jquery_test.errors.count, "error") %> prohibited this jquery_test from being saved:</h2>

      <ul>
      <% @jquery_test.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
